@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
@model List<DoAn.Models.Accounts.User>
<div class="admin-page">
    <!-- Tiêu đề -->
    <div class="admin-header">
        <h2 class="admin-title">Quản lý nhân viên</h2>
    </div>

    <!-- Thanh công cụ -->
    <div class="admin-toolbar">
        <div class="filter-group">
            <div class="filter-item">Tất cả</div>
            <div class="filter-item">Trạng thái</div>
            <div class="filter-item">Sắp xếp</div>
        </div>
        <div class="action-group">
            <button class="btn btn--primary">Thêm mới</button>
        </div>
    </div>

    <!-- Bảng dữ liệu -->
    <table class="table table--dark">
        <thead>
            <tr>
                <th><input type="checkbox" name="name" value="false" /></th>
                <th>Họ và tên</th>
                <th>Số điện thoại</th>
                <th>Email</th>
                <th>Ngày sinh</th>
                <th>Trạng thái</th>
                <th>Thao tác</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var employee in Model)
            {
                <tr>
                    <td><input type="checkbox" name="name" value="false" /></td>
                    @* <td><img src="@employee." height="100" width="100" alt="@movie.Title" /></td> *@
                    <td><p class="movie-title">@employee.FullName</p></td>
                    <td><p class="movie-description">@employee.Phone</p></td>
                    <td><p class="">@employee.Email</p></td>
                    <td><p class="">@employee.Birthday</p></td>
                    <td><p class="">@employee.IsActive</p></td>
                    <td>
                        <div class="actions">
                            <button><i class="bi bi-eye-fill"></i></button>
                            <button><i class="bi bi-pencil-square"></i></button>
                            <button><i class="bi bi-trash3-fill"></i></button>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <td colspan="7">Tổng kết</td>
            </tr>
        </tfoot>
    </table>
</div>
